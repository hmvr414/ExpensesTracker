{
  "name": "expenses-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names api,client --prefix-colors cyan,magenta \"npm run dev --prefix api\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix api && npm run build --prefix client",
    "start": "NODE_ENV=production node api/dist/index.js",
    "migrate": "npm run migrate --prefix api",
    "test": "npm test --prefix api && npm test --prefix client"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
